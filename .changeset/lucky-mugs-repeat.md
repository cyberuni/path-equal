---
'path-equal': patch
---

Move the release pipeline to GitHub OIDC trusted publishing on `cyberuni`, removing the
`NPM_TOKEN` and `CI_GITHUB_TOKEN` dependency, and point the package metadata at the new
repository home.
